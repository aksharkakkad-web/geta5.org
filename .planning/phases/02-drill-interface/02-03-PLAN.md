---
phase: 02-drill-interface
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: false
requirements: [DRILL-01, DRILL-02, DRILL-03, DRILL-04, DRILL-05, DRILL-06, DRILL-09]

must_haves:
  truths:
    - "All 3 views (unit-select, session, results) render correctly at 375px, 768px, and 1280px"
    - "KaTeX renders correctly for formula cards (not literal $...$)"
    - "All 6 drill modes show correct display labels"
    - "Score ring displays correctly at 0%, 50%, and 100%"
    - "Missed cards list renders correctly with user input vs correct answer"
    - "CSS custom properties used everywhere — no hardcoded hex visible"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete drill interface via screenshot loops. Confirm all 3 views render correctly at multiple breakpoints and all acceptance criteria are met.

Purpose: CLAUDE.md Critical Rule #2 — NEVER skip the screenshot loop. UI work is not done until the user confirms it visually. This plan gates Phase 2 completion.

Output: User-confirmed visual sign-off on all drill interface views.
</objective>

<execution_context>
@C:/Users/ninte/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ninte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-drill-interface/02-CONTEXT.md
@.planning/phases/02-drill-interface/02-01-SUMMARY.md
@.planning/phases/02-drill-interface/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight validation — run full test suite and dev server check</name>
  <files></files>
  <read_first>
    - utils/__tests__/drillSession.test.ts (verify tests exist)
    - public/data/ap-psychology/drills/unit-1.json (verify fixture exists)
  </read_first>
  <action>
    Run the following checks before screenshot loops:

    1. `npm test` — full test suite must be green (all drillSession tests + existing tests)
    2. `npx tsc --noEmit` — zero TypeScript errors
    3. `npm run dev` — confirm dev server starts without errors (check first 10 seconds of output)
    4. Verify `public/data/ap-psychology/drills/unit-1.json` exists and has 5 cards

    If any check fails, fix the issue before proceeding to screenshot loops. Common fixes:
    - Import path issues: ensure all `@/` imports resolve correctly
    - Missing exports: verify `utils/drillSession.ts` exports all expected symbols
    - CSS class issues: verify `app/globals.css` has the `score-ring` classes
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - `npx tsc --noEmit` exits with code 0
    - `npm run dev` starts without crash (process stays alive for 5+ seconds)
  </acceptance_criteria>
  <done>Full test suite green, TypeScript clean, dev server starts successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Screenshot loop — Unit Selector view</name>
  <files></files>
  <action>
    Present the unit selector view to the user for visual verification. Start dev server if not running (`npm run dev`). Direct user to http://localhost:3000/ap-psychology/drills. Request screenshot at 1280px, 768px, and 375px widths. Verify against CONTEXT.md Unit Selector View spec: gradient art areas, card counts, mastery bars, "Coming soon" for missing units, Study All card, hover effects.
  </action>
  <what-built>Unit selector view with gradient art cards, card counts, mastery bars, Study All card, and 404 "Coming soon" handling. Navigate to http://localhost:3000/ap-psychology/drills in your browser.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/ap-psychology/drills at desktop width (1280px)
    2. Confirm: "Study All" card is at top, full-width, accent-bordered, shows "5 cards" badge
    3. Confirm: Unit 1 card shows gradient art area with emoji, "UNIT 1" label, "Biological Bases of Behavior" title, "5 cards" badge, mastery bar at 0%
    4. Confirm: Units 2-8 show "Coming soon" in muted style, appear disabled/non-clickable
    5. Hover over Unit 1 card — confirm it lifts slightly (translateY) and border highlights
    6. Resize to 768px — confirm grid becomes 2-column
    7. Resize to 375px — confirm grid becomes 1-column
    8. Confirm: no hardcoded hex colors visible, all colors match dark theme
    9. Take a screenshot and paste it for verification
  </how-to-verify>
  <verify>User confirms screenshot matches spec</verify>
  <acceptance_criteria>
    - User approves the unit selector view at all 3 breakpoints
    - Study All card visible at top with accent border
    - Unit 1 shows gradient art, card count, mastery bar
    - Units 2-8 show "Coming soon" disabled state
  </acceptance_criteria>
  <done>User types "approved" for unit selector view.</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Screenshot loop — Drill Session view (all 4 card states)</name>
  <files></files>
  <action>
    Guide user through drill session to capture all 4 card states. Direct user to click Unit 1 from the unit selector. Walk through idle, typing, correct, and wrong states. Verify KaTeX rendering on formula card. Check score badges update live in session header.
  </action>
  <what-built>Drill session with card progression, 4-state DrillCard, fuzzy matching, KaTeX rendering, and live score tracking.</what-built>
  <how-to-verify>
    1. From the unit selector, click Unit 1 to start a drill session
    2. Idle state: Confirm mode tag shows (e.g. "DEFINITION -> TERM"), prompt text visible, input empty, Check Answer button disabled
    3. Typing state: Type a character — confirm input border turns accent color, Check Answer button active
    4. Correct state: Type "Psychology" for first card — confirm green feedback panel
    5. Wrong state: On next card, type "wrong answer" — confirm red feedback panel with user input and correct answer
    6. KaTeX card: Progress to formula card — confirm math renders as notation, NOT literal dollar signs
    7. Score badges: Confirm header shows progress bar, counter, green/red pills updating
    8. Session header: Confirm unit label on left side
    9. Take screenshots of each card state
  </how-to-verify>
  <verify>User confirms all 4 card states render correctly</verify>
  <acceptance_criteria>
    - User approves all 4 card states (idle, typing, correct, wrong)
    - KaTeX formula renders as math notation
    - Score badges update live
    - Session header shows correct unit label
  </acceptance_criteria>
  <done>User types "approved" for drill session view.</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Screenshot loop — Results view and full flow verification</name>
  <files></files>
  <action>
    Guide user through completing a full session and verifying the results screen. Check score ring, contextual heading, missed cards list, all 3 CTAs. Verify retry flow, back navigation, study-another-unit flow. Have user check localStorage in DevTools for mastery data.
  </action>
  <what-built>Results screen with score ring, contextual heading, missed cards list, and all 3 CTAs. Also verifying localStorage writes and analytics.</what-built>
  <how-to-verify>
    1. Complete all 5 cards in the session (get some right, some wrong)
    2. Results screen: Confirm "Session Complete" title, unit name subtitle
    3. Score ring: Confirm conic gradient circle with percentage and "score" label inside
    4. Contextual heading: Confirm heading matches your score tier
    5. Missed cards: Confirm section shows with red count badge, each card shows answer + truncated prompt + "you: [input]"
    6. CTAs: Confirm "Retry missed cards (N)" button, "Back to AP Psychology" button, "Study another unit" link
    7. Retry flow: Click "Retry missed cards" — confirm only wrong cards appear, header shows "Retry" prefix
    8. Back navigation: Click "Back to AP Psychology" — confirm navigation to subject hub
    9. Study another unit: Start session, complete it, click "Study another unit" — confirm return to unit selector
    10. localStorage check: Open DevTools > Application > Local Storage. Confirm mastery and totalQuestions keys exist
    11. Take screenshots
  </how-to-verify>
  <verify>User confirms results view and full flow work correctly</verify>
  <acceptance_criteria>
    - User approves results screen layout (score ring, heading, missed cards, CTAs)
    - Retry flow works (only wrong cards, isRetry prefix)
    - Back navigation works (returns to subject hub)
    - Study another unit works (returns to unit selector)
    - localStorage contains mastery data after session
  </acceptance_criteria>
  <done>User types "approved" for results view and full flow. Phase 2 ready for completion.</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. All 3 screenshot loops approved by user
2. Full test suite green (`npm test`)
3. Zero TypeScript errors (`npx tsc --noEmit`)
4. All 24 acceptance criteria from CONTEXT.md verified
</verification>

<success_criteria>
- User has visually confirmed all 3 views at desktop and mobile breakpoints
- All 4 drill card states render correctly with correct colors and feedback
- KaTeX renders math notation (not literal $...$)
- Score ring displays correctly
- localStorage mastery data written correctly
- Retry and Study All flows work end-to-end
- Phase 2 can be marked complete in CLAUDE.md Phase Tracker
</success_criteria>

<output>
After completion, create `.planning/phases/02-drill-interface/02-03-SUMMARY.md`
Then update Phase Tracker in CLAUDE.md: Phase 2 status = "Complete"
</output>
