---
phase: 05-practice-test-interface
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/test/TestSetup.tsx
  - components/test/TestTimer.tsx
  - components/test/TestNavGrid.tsx
  - components/test/TestSession.tsx
  - components/test/TestResults.tsx
  - app/[subject]/practice-test/page.tsx
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "Student sees setup screen with subject name, question count, duration, timed toggle, and Start Test button"
    - "Timer counts down from AP-accurate duration and displays MM:SS format"
    - "Timer turns red at less than 5 minutes remaining"
    - "Timer auto-submits on expiry with Time's Up overlay"
    - "Question nav grid shows answered/unanswered/flagged/current states with correct colors"
    - "Student can jump to any question via nav grid or prev/next arrows"
    - "Student can flag questions for review"
    - "Submit Test shows inline confirmation when questions are unanswered"
    - "Results screen shows score ring, projected AP score, per-unit breakdown, missed questions, and CTAs"
    - "localStorage is written on completion (score + totalQuestions) but NOT mastery"
    - "Analytics test_completed event fires on completion"
  artifacts:
    - path: "components/test/TestSetup.tsx"
      provides: "Setup view with timed/timer toggles and Start Test CTA"
      min_lines: 60
    - path: "components/test/TestTimer.tsx"
      provides: "Countdown timer with warning state and auto-submit"
      min_lines: 40
    - path: "components/test/TestNavGrid.tsx"
      provides: "Question number grid with state indicators"
      min_lines: 50
    - path: "components/test/TestSession.tsx"
      provides: "Session orchestrator with header, nav, MCQCard, prev/next"
      min_lines: 100
    - path: "components/test/TestResults.tsx"
      provides: "Score ring, AP score, per-unit breakdown, missed questions, CTAs"
      min_lines: 100
    - path: "app/[subject]/practice-test/page.tsx"
      provides: "3-view orchestrator: setup -> session -> results"
      min_lines: 60
  key_links:
    - from: "app/[subject]/practice-test/page.tsx"
      to: "utils/testSession.ts"
      via: "imports TestSessionState, composeTest"
      pattern: "import.*composeTest.*testSession"
    - from: "app/[subject]/practice-test/page.tsx"
      to: "utils/testConfig.ts"
      via: "imports AP_TEST_CONFIG"
      pattern: "import.*AP_TEST_CONFIG.*testConfig"
    - from: "components/test/TestSession.tsx"
      to: "components/mcq/MCQCard.tsx"
      via: "renders MCQCard for question display"
      pattern: "import.*MCQCard"
    - from: "components/test/TestResults.tsx"
      to: "utils/testSession.ts"
      via: "calls handleTestComplete and computePerUnitAccuracy"
      pattern: "handleTestComplete|computePerUnitAccuracy"
    - from: "components/test/TestTimer.tsx"
      to: "TestSession onExpiry callback"
      via: "calls onExpiry when seconds reach 0"
      pattern: "onExpiry"
---

<objective>
Build all 5 test UI components and the page orchestrator that compose the full practice test experience.

Purpose: The student-facing interface -- setup, timed session with free navigation, and rich results. Reuses MCQCard and StimulusRenderer from Phase 3.
Output: 5 new components in `components/test/`, updated page orchestrator at `app/[subject]/practice-test/page.tsx`.

**Route override (D-01):** CONTEXT.md D-01 specifies `app/[subject]/test/page.tsx`, but the codebase already has a stub at `app/[subject]/practice-test/page.tsx` and the subject hub (`app/[subject]/page.tsx`) links to `/${subject.slug}/practice-test`. Per RESEARCH.md Pitfall 3, using `practice-test` to match existing navigation. This is the correct route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-practice-test-interface/05-CONTEXT.md
@.planning/phases/05-practice-test-interface/05-RESEARCH.md
@.planning/phases/05-practice-test-interface/05-UI-SPEC.md
@.planning/phases/05-practice-test-interface/05-01-SUMMARY.md

@utils/testConfig.ts
@utils/testSession.ts
@utils/mcqSession.ts
@utils/localStorage.ts
@utils/scoring.ts
@utils/scramble.ts
@utils/analytics.ts
@utils/subjects.ts
@components/mcq/MCQCard.tsx
@components/mcq/MCQResults.tsx
@components/mcq/StimulusRenderer.tsx
@app/[subject]/practice/page.tsx
@app/[subject]/practice-test/page.tsx
@app/globals.css
@design-system/ascendly/MASTER.md

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From utils/testConfig.ts (created in Plan 01):
```typescript
export interface TestConfig {
  questionCount: number
  durationMinutes: number
}
export const AP_TEST_CONFIG: Record<string, TestConfig>
```

From utils/testSession.ts (created in Plan 01):
```typescript
export interface TestAnswer {
  selectedChoiceId: string
  isCorrect: boolean
}

export interface TestSessionState {
  questions: MCQ[]
  answers: Record<string, TestAnswer>
  flagged: Record<string, boolean>
  currentIndex: number
  timed: boolean
  showTimer: boolean
  durationSeconds: number
  subjectSlug: string
}

export interface PerUnitResult {
  unitNumber: string
  unitName: string
  correct: number
  total: number
  accuracy: number
}

export function composeTest(questionsByUnit: Map<string, MCQ[]>, targetCount: number): MCQ[]
export function computePerUnitAccuracy(questions: MCQ[], answers: Record<string, TestAnswer>): PerUnitResult[]
export function handleTestComplete(session: TestSessionState, subject: string): void
```

From utils/mcqSession.ts:
```typescript
export interface MCQ { id: string; unit: string; subject: string; difficulty: 'easy' | 'medium' | 'hard'; stimulus: MCQStimulus; question: string; choices: MCQChoice[]; unit_objective: string }
export interface MCQAnswer { selectedChoiceId: string; isCorrect: boolean }
```

From components/mcq/MCQCard.tsx:
```typescript
interface MCQCardProps {
  question: MCQ
  onAnswer: (questionId: string, selectedChoiceId: string, isCorrect: boolean) => void
  onNext: () => void
}
```

From utils/subjects.ts:
```typescript
export interface Subject { slug: string; name: string; examDate: string; units: SubjectUnit[] }
export function getSubject(slug: string): Subject | undefined
```

From utils/scoring.ts:
```typescript
export function projectScore(rawAccuracy: number): 1 | 2 | 3 | 4 | 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestSetup, TestTimer, and TestNavGrid components</name>
  <files>components/test/TestSetup.tsx, components/test/TestTimer.tsx, components/test/TestNavGrid.tsx</files>
  <read_first>
    - app/[subject]/practice-test/page.tsx (current stub to replace)
    - .planning/phases/05-practice-test-interface/05-UI-SPEC.md (View 1 setup spec, timer spec, nav grid spec)
    - design-system/ascendly/MASTER.md (design tokens, spacing, animation rules)
    - components/mcq/MCQCard.tsx (answer card pattern to understand choice rendering)
    - utils/testConfig.ts (AP_TEST_CONFIG for setup display)
    - utils/testSession.ts (TestSessionState, TestAnswer types)
    - app/globals.css (CSS custom properties, score-ring class)
  </read_first>
  <action>
    Create `components/test/` directory with three components. All components are `'use client'`.

    **TestSetup.tsx** -- Per D-02 setup view, D-11 timed toggle, D-12 timer toggle:
    - Props: `{ subjectSlug: string; subjectName: string; questionCount: number; durationMinutes: number; onStart: (timed: boolean, showTimer: boolean) => void }`
    - State: `timed` (default true per D-11), `showTimer` (default true per D-12)
    - Layout: Centered card, `max-width: 480px`, `var(--bg-card)` background, `var(--bg-border)` border, `var(--radius-lg)` border-radius, 32px padding
    - Elements top to bottom:
      1. Subject name: 24px weight 700
      2. "Practice Test" subtitle: 14px weight 400, `var(--text-secondary)`
      3. Metadata row: Lucide `BookOpen` + `{questionCount} questions` and Lucide `Clock` + `{durationMinutes} minutes` in 14px `var(--text-muted)`, gap 16px
      4. 1px divider `var(--bg-border)`
      5. Timed Mode toggle row: "Timed Mode" label + pill toggle (44px wide x 24px tall, 20px thumb). ON = `var(--accent)` track + white thumb. OFF = `var(--bg-border)` track + `var(--text-muted)` thumb. Transition 150ms ease.
      6. Show Timer toggle row: "Show Timer" label + pill toggle. Disabled (opacity 0.4, cursor not-allowed) when timed is false.
      7. 1px divider
      8. "Start Test" button: full-width, `var(--accent)` background, white text, padding 14px 20px, `var(--radius-md)` border-radius
    - On "Start Test" click: call `onStart(timed, showTimer)`
    - Accessibility: toggle inputs are `<button role="switch" aria-checked={value}>` elements

    **TestTimer.tsx** -- Per D-13, D-14, D-15, D-16:
    - Props: `{ initialSeconds: number; timed: boolean; visible: boolean; onExpiry: () => void }`
    - If `!timed`: render nothing, create no interval
    - State: `secondsLeft` initialized from `initialSeconds`
    - useRef for intervalId (prevents memory leak per Pitfall 7)
    - useEffect creates setInterval(1000ms) when timed is true. Decrements `secondsLeft`. When result is 0: clearInterval, call `onExpiry()` immediately (per Pitfall 4 -- decrement first, then check).
    - useEffect cleanup clears interval on unmount.
    - Format: `MM:SS` with `padStart(2, '0')` for both parts
    - Style: 24px weight 700, `font-variant-numeric: tabular-nums`
    - When `secondsLeft <= 300`: color transitions to `var(--accent-danger)` with 300ms ease. Add CSS pulse animation (opacity 1->0.6->1, 1200ms loop) with `@media (prefers-reduced-motion: reduce)` disabling it.
    - When `!visible`: container has `visibility: hidden` (interval still runs, timer still counts -- per D-12 spec and UI-SPEC interaction contract)
    - Expose `secondsLeft` via a ref or callback for parent to read (for sessionStorage guard)

    **TestNavGrid.tsx** -- Per D-06, D-07, D-08:
    - Props: `{ totalQuestions: number; currentIndex: number; answers: Record<string, TestAnswer>; flagged: Record<string, boolean>; questionIds: string[]; onJump: (index: number) => void }`
    - Renders a CSS grid: 10 columns on desktop (>= 768px), 8 on mobile
    - Each cell: `<button>` with min-height 44px, min-width 36px, `var(--radius-sm)` border-radius
    - Cell states (colors from UI-SPEC Color section):
      - Current (active): `var(--accent)` solid background, white numeral, 14px weight 400
      - Answered: `color-mix(in srgb, var(--accent) 20%, transparent)` background, `var(--accent)` border, `var(--text-primary)` numeral
      - Flagged: `color-mix(in srgb, var(--accent-warning) 15%, transparent)` background, `var(--accent-warning)` border, numeral in warning color. Lucide `Flag` 12px overlaid bottom-right
      - Unanswered: `var(--bg-card)` background, `var(--bg-border)` border, `var(--text-muted)` numeral
    - State transitions: 150ms ease on background-color and border-color
    - onClick: call `onJump(index)`
    - Mobile (< 768px): collapse to horizontal scrolling strip showing current +/- 4 question numbers (use `overflow-x: auto`, `white-space: nowrap`, auto-scroll to keep current centered)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/test/TestSetup.tsx exists and contains `'use client'`
    - components/test/TestSetup.tsx contains `onStart(timed, showTimer)` call
    - components/test/TestSetup.tsx contains `role="switch"` (accessible toggle)
    - components/test/TestSetup.tsx contains `Timed Mode` and `Show Timer` labels
    - components/test/TestSetup.tsx contains `Start Test` button text
    - components/test/TestTimer.tsx exists and contains `setInterval`
    - components/test/TestTimer.tsx contains `clearInterval` in cleanup
    - components/test/TestTimer.tsx contains `font-variant-numeric: tabular-nums`
    - components/test/TestTimer.tsx contains `padStart(2, '0')`
    - components/test/TestTimer.tsx contains `onExpiry()` call when seconds reach 0
    - components/test/TestTimer.tsx contains `prefers-reduced-motion` media query or inline check
    - components/test/TestNavGrid.tsx exists and renders a grid of numbered buttons
    - components/test/TestNavGrid.tsx contains `color-mix` for answered and flagged states
    - components/test/TestNavGrid.tsx contains `Flag` import from lucide-react
    - `npx tsc --noEmit` reports no errors in these 3 files
  </acceptance_criteria>
  <done>TestSetup renders setup card with toggles and Start CTA. TestTimer counts down with warning state and auto-submit. TestNavGrid shows question states in a responsive grid. All type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: TestSession, TestResults, and page orchestrator</name>
  <files>components/test/TestSession.tsx, components/test/TestResults.tsx, app/[subject]/practice-test/page.tsx</files>
  <read_first>
    - components/test/TestSetup.tsx (created in Task 1)
    - components/test/TestTimer.tsx (created in Task 1)
    - components/test/TestNavGrid.tsx (created in Task 1)
    - components/mcq/MCQCard.tsx (reused for question rendering)
    - components/mcq/MCQResults.tsx (score ring pattern to extend)
    - components/mcq/StimulusRenderer.tsx (stimulus rendering)
    - app/[subject]/practice/page.tsx (3-view orchestrator pattern to mirror)
    - app/[subject]/page.tsx (subject hub -- verify Practice Test mode card href points to /${subject}/practice-test)
    - utils/testSession.ts (TestSessionState, handleTestComplete, computePerUnitAccuracy, composeTest)
    - utils/testConfig.ts (AP_TEST_CONFIG)
    - utils/subjects.ts (getSubject for display names)
    - utils/scoring.ts (projectScore)
    - utils/scramble.ts (scramble for question + choice ordering)
    - app/globals.css (score-ring CSS, CSS custom properties)
    - .planning/phases/05-practice-test-interface/05-UI-SPEC.md (View 2 session spec, View 3 results spec, all interaction contracts)
    - design-system/ascendly/MASTER.md (design tokens)
  </read_first>
  <action>
    **TestSession.tsx** -- Per D-06 through D-17, UI-SPEC View 2:
    - Props: `{ session: TestSessionState; subject: string; subjectName: string; onComplete: (session: TestSessionState) => void }`
    - Internal state: `answers: Record<string, TestAnswer>` (initialized from session.answers), `flagged: Record<string, boolean>` (initialized from session.flagged), `currentIndex: number`, `showTimer: boolean` (from session.showTimer), `showSubmitConfirm: boolean`
    - **answersRef pattern:** Use `useRef` mirroring answers state to prevent stale closure in timer's onExpiry callback (Pitfall 1). Update ref on every answer change.
    - **Session header** (sticky, `var(--bg-secondary)` background, 1px bottom border, padding 12px 16px):
      - Left: Subject name + "Practice Test" -- 14px `var(--text-secondary)`
      - Center: `<TestTimer>` component (pass timed, showTimer, durationSeconds, onExpiry)
      - Right: Eye/EyeOff toggle button with `aria-label` ("Show timer"/"Hide timer" per UI-SPEC) + correct/wrong tally badges using `color-mix` pattern from MCQSession
    - **TestNavGrid** below header (per D-07)
    - **Question area** (max-width 720px centered):
      - "Question N of M" counter -- 14px `var(--text-muted)`
      - **Flag for Review button**: Lucide `Flag` + "Flag for Review" / "Flagged" text. Ghost style, toggles `flagged[questionId]`. `var(--text-muted)` default, `var(--accent-warning)` when flagged. Per D-08.
      - `<MCQCard question={currentQuestion} onAnswer={handleAnswer} onNext={handleNavNext} />` -- MCQCard reused as-is. `onAnswer` updates answers map (no auto-advance per research anti-pattern). `onNext` navigates to next question (convenience per research Open Question 1).
      - **Navigation row**: Lucide `ChevronLeft`/`ChevronRight` prev/next buttons (44px touch target, ghost style, disabled at boundaries with opacity 0.4 + cursor not-allowed) + "Submit Test" button (primary accent pill, per D-09)
    - **Submit flow** (per D-09, D-10):
      - If all questions answered: submit immediately on button click
      - If unanswered questions > 0: show inline confirmation banner (not modal). Banner: `var(--bg-card)` bg, `var(--accent-warning)` 4px left border, padding 12px 16px. Text: "N questions unanswered. Unanswered questions will be marked wrong." Buttons: "Submit Anyway" (accent-danger style) + "Keep Going" (ghost).
      - On timer expiry (per D-10, D-16): auto-submit immediately, no confirmation. Show "Time's Up" overlay: full-screen `var(--bg-primary)` at 0.9 opacity, "Time's Up" text 28px weight 700, 1500ms display then transition to results.
    - **MCQCard key prop**: Use `currentQuestion.id` as React key so MCQCard resets state when navigating between questions. This is critical -- without it, MCQCard retains the previous question's selected/submitted state.
    - **Desktop layout (>= 1024px)**: Left sidebar for TestNavGrid at fixed 240px width, main area for question card.
    - **Mobile (< 768px)**: Nav grid collapses to scrolling strip (handled by TestNavGrid), timer below nav strip per UI-SPEC.
    - On submit: call `onComplete({ ...session, answers: answersRef.current, flagged })`

    **TestResults.tsx** -- Per D-18 through D-26, UI-SPEC View 3:
    - Props: `{ session: TestSessionState; subject: string; onRetake: () => void }`
    - **completedRef guard** (per Pitfall 2): useRef boolean, fires `handleTestComplete(session, subject)` only once in useEffect.
    - Computes:
      - `correctCount` from answers with `isCorrect === true`
      - `totalQuestions` from `session.questions.length`
      - `pct = Math.round((correctCount / totalQuestions) * 100)`
      - `projectedScore = projectScore(correctCount / totalQuestions)` per D-19
      - `perUnitResults = computePerUnitAccuracy(session.questions, session.answers)` per D-20
      - `missedQuestions = session.questions.filter(q => !session.answers[q.id]?.isCorrect)` per D-21
    - Layout: Centered column, max-width 640px, gap 32px.
    - Sections (per UI-SPEC View 3):
      1. **Title**: "Test Complete" 24px weight 700 + subject name 14px `var(--text-secondary)`
      2. **Score ring**: Reuse `.score-ring` + `.score-ring-inner` CSS classes from globals.css. Set `--score-deg` CSS variable to `(pct / 100) * 360` degrees. Percentage inside ring, "score" label below.
      3. **Score metadata**: "N / M correct . Projected AP Score: X" -- 14px `var(--text-secondary)`. AP score as pill badge with color based on score value:
         - Score 5: `var(--accent-success)` tint
         - Score 4: `var(--accent)` tint
         - Score 3: `var(--accent-warning)` tint
         - Score 1-2: `var(--accent-danger)` tint
      4. **Per-unit breakdown card** (per D-20): `var(--bg-card)` border card, "Unit Breakdown" heading. Each row: unit name (14px weight 700) + accuracy bar (4px height, `var(--mastery-empty)` track / `var(--mastery-fill)` fill, max-width 200px, `transition: width 400ms ease`) + percentage (14px `var(--text-muted)` right-aligned).
      5. **Missed questions card** (per D-21): "Missed Questions" heading. Top 5 shown: question text + student's answer + correct answer. If more: "+ N more" text. Same pattern as MCQResults.
      6. **CTA stack** (per D-22, gap 12px):
         - "Retake Test": full-width primary accent, Lucide `RotateCcw`
         - "Practice by Unit": full-width ghost (border `var(--bg-border)`, hover `var(--accent)`), Lucide `BookOpen`, navigates to `/${subject}/practice`
         - "Back to [Subject Name]": full-width ghost, Lucide `ArrowLeft`, navigates to `/${subject}`
    - subjectName from `getSubject(subject)?.name ?? subject` (per UI-SPEC pre-delivery checklist)

    **page.tsx orchestrator** -- Per D-02, D-03 (route at `app/[subject]/practice-test/page.tsx` per RESEARCH.md Pitfall 3, overriding D-01's `test` path):
    - `'use client'` directive
    - `use(params)` to unwrap Promise params (per Next.js 15+ pattern, established in practice/page.tsx)
    - State: `view: TestView` ('setup' | 'session' | 'results'), `session: TestSessionState | null`
    - On mount: load subject data via `getSubject(subject)`, get config via `AP_TEST_CONFIG[subject]`
    - **Data loading**: `Promise.allSettled` to fetch all unit MCQ JSON files: `/data/${subject}/mcq/unit-${n}.json` for each unit number from subject data. 404s silently become null (established pattern per research). Group results into `Map<string, MCQ[]>` keyed by unit slug. Call `composeTest(questionsByUnit, config.questionCount)` to build test.
    - If zero questions load: show empty state per UI-SPEC copywriting: "No questions available for this subject yet. Content is being added."
    - **handleStart(timed, showTimer)**: Build TestSessionState from composed questions. Set `durationSeconds: config.durationMinutes * 60`. Set view to 'session'.
    - **handleComplete(finalSession)**: Set session, set view to 'results'.
    - **handleRetake()**: Re-compose test (re-shuffle), reset to 'setup'.
    - Conditional rendering by view state (same pattern as practice/page.tsx).
    - Session view: full-width main (no max-width constraint on outer container), min-height calc(100dvh - 64px). Setup/results: max-width 960px centered.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/test/TestSession.tsx exists and contains `'use client'`
    - components/test/TestSession.tsx contains `MCQCard` import
    - components/test/TestSession.tsx contains `TestNavGrid` import
    - components/test/TestSession.tsx contains `TestTimer` import
    - components/test/TestSession.tsx contains `answersRef` (stale closure guard per Pitfall 1)
    - components/test/TestSession.tsx contains `aria-label` on timer toggle button
    - components/test/TestSession.tsx contains `Submit Test` button text
    - components/test/TestSession.tsx contains `Time's Up` overlay text
    - components/test/TestSession.tsx contains `questions unanswered` confirmation text
    - components/test/TestResults.tsx exists and contains `'use client'`
    - components/test/TestResults.tsx contains `completedRef` (double-fire guard per Pitfall 2)
    - components/test/TestResults.tsx contains `handleTestComplete` import
    - components/test/TestResults.tsx contains `computePerUnitAccuracy` import
    - components/test/TestResults.tsx contains `projectScore` import
    - components/test/TestResults.tsx contains `score-ring` CSS class usage
    - components/test/TestResults.tsx contains `Unit Breakdown` heading text
    - components/test/TestResults.tsx contains `Missed Questions` heading text
    - components/test/TestResults.tsx contains `Retake Test` button text
    - components/test/TestResults.tsx contains `Practice by Unit` button text
    - components/test/TestResults.tsx contains `Back to` CTA text
    - app/[subject]/practice-test/page.tsx contains `'use client'`
    - app/[subject]/practice-test/page.tsx contains `use(params)` (Next.js 15+ pattern)
    - app/[subject]/practice-test/page.tsx contains `Promise.allSettled` (data loading)
    - app/[subject]/practice-test/page.tsx contains `composeTest` import
    - app/[subject]/practice-test/page.tsx contains `AP_TEST_CONFIG` import
    - app/[subject]/practice-test/page.tsx contains `TestSetup` import
    - app/[subject]/practice-test/page.tsx contains `TestSession` import
    - app/[subject]/practice-test/page.tsx contains `TestResults` import
    - app/[subject]/page.tsx mode card for Practice Test links to `/${subject}/practice-test` (verified via read_first)
    - `npx tsc --noEmit` reports no errors in these files
  </acceptance_criteria>
  <done>Full practice test flow works: setup screen with toggles -> timed session with question navigation, flagging, and MCQCard reuse -> results with score ring, AP score, per-unit breakdown, missed questions, and CTAs. Page orchestrator loads MCQ data and composes test with proportional sampling. Subject hub mode card href confirmed pointing to /practice-test.</done>
</task>

</tasks>

<verification>
```bash
# Type-check entire project
npx tsc --noEmit --project tsconfig.json

# Verify all test utilities still pass
npx jest utils/__tests__/testConfig.test.ts utils/__tests__/testSession.test.ts -x

# Dev server builds without errors
npx next build 2>&1 | tail -20
```
</verification>

<success_criteria>
- All 5 test components + page orchestrator created and type-check cleanly
- Page loads at /[subject]/practice-test with setup view
- Setup shows timed/untimed toggle with AP-accurate metadata
- Session renders MCQCard with free question navigation via grid and arrows
- Timer counts down and auto-submits at 0 with overlay
- Results show score ring, projected AP score, per-unit breakdown, missed questions, and 3 CTAs
- localStorage written on completion (score + totalQuestions but NOT mastery)
- Analytics event fires on completion
- Subject hub mode card href verified pointing to /${subject}/practice-test
- Project builds with `next build`
</success_criteria>

<output>
After completion, create `.planning/phases/05-practice-test-interface/05-02-SUMMARY.md`
</output>
