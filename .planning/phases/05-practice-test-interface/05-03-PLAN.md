---
phase: 05-practice-test-interface
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "All 3 views render correctly with real content"
    - "Timer counts down and warning state is visible"
    - "Question navigation works across all states"
    - "Results screen displays all required sections"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete practice test interface via screenshot loop.

Purpose: Critical Rule #2 enforcement -- UI work is not complete until visually verified by the user.
Output: User approval of all 3 views (setup, session, results).

**Route note:** The practice test lives at `app/[subject]/practice-test/page.tsx` (not `test/` as D-01 originally specified). This matches the existing subject hub navigation per RESEARCH.md Pitfall 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-practice-test-interface/05-CONTEXT.md
@.planning/phases/05-practice-test-interface/05-UI-SPEC.md
@.planning/phases/05-practice-test-interface/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Screenshot verification of practice test interface</name>
  <files>components/test/TestSetup.tsx, components/test/TestSession.tsx, components/test/TestResults.tsx, app/[subject]/practice-test/page.tsx</files>
  <action>
    Start the dev server with `npm run dev`. Navigate to http://localhost:3000/ap-psychology and click "Practice Test". Walk through the full flow: setup view -> start test -> answer questions, flag one, navigate via grid and arrows -> submit with unanswered questions (verify confirmation banner) -> review results screen.

    If no MCQ content JSON exists yet for AP Psychology, create a temporary fixture at `public/data/ap-psychology/mcq/unit-1.json` with 3-5 test MCQs matching the schema to verify the full flow.

    Capture screenshots of:
    1. Setup view (with both toggles visible)
    2. Session view (showing timer, nav grid, question card)
    3. Submit confirmation banner (when unanswered questions remain)
    4. Results view (score ring, AP score, per-unit breakdown, missed questions, CTAs)

    Present screenshots to user for approval per Critical Rule #2 screenshot loop.
  </action>
  <verify>User confirms all views match the 05-UI-SPEC.md design contract visually and functionally.</verify>
  <done>User types "approved" confirming all 3 views render correctly with proper styling, timer functionality, navigation, and results display.</done>
  <read_first>
    - .planning/phases/05-practice-test-interface/05-UI-SPEC.md
    - components/test/TestSetup.tsx
    - components/test/TestSession.tsx
    - components/test/TestResults.tsx
  </read_first>
  <acceptance_criteria>
    - User has seen screenshots of all 3 views (setup, session, results)
    - User has confirmed approval
    - No visual regressions reported
  </acceptance_criteria>
</task>

</tasks>

<verification>
User visual confirmation of all 3 practice test views.
</verification>

<success_criteria>
- User approves setup, session, and results views
- All UI elements match the 05-UI-SPEC.md contract
- Timer, navigation, flagging, and submit flows work correctly
- Results show accurate score data
</success_criteria>

<output>
After completion, create `.planning/phases/05-practice-test-interface/05-03-SUMMARY.md`
</output>
