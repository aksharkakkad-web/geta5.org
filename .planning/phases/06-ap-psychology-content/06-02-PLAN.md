---
phase: 06-ap-psychology-content
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - public/data/ap-psychology/meta.json
  - public/data/ap-psychology/drills/unit-1.json
  - public/data/ap-psychology/drills/unit-2.json
  - public/data/ap-psychology/drills/unit-3.json
  - public/data/ap-psychology/drills/unit-4.json
  - public/data/ap-psychology/drills/unit-5.json
  - public/data/ap-psychology/drills/unit-6.json
  - public/data/ap-psychology/drills/unit-7.json
  - public/data/ap-psychology/drills/unit-8.json
  - public/data/ap-psychology/mcq/unit-1.json
  - public/data/ap-psychology/mcq/unit-2.json
  - public/data/ap-psychology/mcq/unit-3.json
  - public/data/ap-psychology/mcq/unit-4.json
  - public/data/ap-psychology/mcq/unit-5.json
  - public/data/ap-psychology/mcq/unit-6.json
  - public/data/ap-psychology/mcq/unit-7.json
  - public/data/ap-psychology/mcq/unit-8.json
  - public/data/ap-psychology/study-guide/unit-1.json
  - public/data/ap-psychology/study-guide/unit-2.json
  - public/data/ap-psychology/study-guide/unit-3.json
  - public/data/ap-psychology/study-guide/unit-4.json
  - public/data/ap-psychology/study-guide/unit-5.json
  - public/data/ap-psychology/study-guide/unit-6.json
  - public/data/ap-psychology/study-guide/unit-7.json
  - public/data/ap-psychology/study-guide/unit-8.json
autonomous: true
requirements: [PSY-01, PSY-02, PSY-03, PSY-04]

must_haves:
  truths:
    - "meta.json exists with all units, CB percentages, learning objectives"
    - "Drill JSON files exist for every unit with ≥20 cards covering all testable terms, psychologists, and application scenarios"
    - "MCQ JSON files exist for every unit with 50–100 questions and 20/45/35 difficulty split"
    - "Study guide JSON files exist for every unit"
    - "All content passes quality gates G1–G8, G8b, G8c, G8d"
    - "Reviewer subagent (Sonnet) has signed off — no outstanding gate failures"
    - "No alternate_answers field anywhere in output"
    - "8–15 is_key_term cards per unit, none on concept_mc cards"
  artifacts:
    - path: "public/data/ap-psychology/meta.json"
      provides: "Subject metadata with all units"
    - path: "public/data/ap-psychology/drills/unit-1.json"
      provides: "Unit 1 drill cards (definition_to_term, significance_to_person, concept_mc)"
      contains: "cards array with id, unit, subject, mode, prompt, answer, difficulty, is_key_term"
    - path: "public/data/ap-psychology/mcq/unit-1.json"
      provides: "Unit 1 MCQ questions (50–100, ~70% with stimulus)"
      exports: ["questions"]
  key_links:
    - from: ".planning/phases/06-ap-psychology-content/PLAN.md"
      to: "public/data/ap-psychology/drills/*.json"
      via: "Writer reads per-unit drill coverage list from PLAN.md"
      pattern: "Every term in PLAN.md drill coverage list appears as a card in unit file"
    - from: "data/schemas/drill.schema.json"
      to: "public/data/ap-psychology/drills/*.json"
      via: "Writer validates every card against schema before writing"
      pattern: "No additionalProperties in any card (schema has additionalProperties: false)"
    - from: "public/data/ap-psychology/drills/*.json"
      to: "public/data/ap-psychology/study-guide/*.json"
      via: "Study guide key terms sourced from is_key_term: true drill cards"
      pattern: "is_key_term: true on 8-15 cards per unit"
---

<objective>
Run the Writer and Reviewer pipeline steps for AP Psychology. The Writer produces all JSON content files (meta.json + drills, MCQs, and study guides for every unit). The Reviewer validates all quality gates and signs off before commit.

Purpose: Generate the complete AP Psychology content library that powers the drill, MCQ, study guide, and practice test interfaces for this subject.

Output: All public/data/ap-psychology/ JSON files, validated against schemas and quality gates, reviewed by Sonnet Reviewer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ap-psychology-content/06-CONTEXT.md
@.planning/phases/06-ap-psychology-content/RESEARCH.md
@.planning/phases/06-ap-psychology-content/PLAN.md
@.planning/CONTENT-WAVES.md
@CLAUDE.md
@docs/PRD.md
@data/schemas/drill.schema.json
@data/schemas/study-guide.schema.json
@docs/superpowers/specs/2026-03-26-drill-mcq-redesign.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Writer — Generate all AP Psychology JSON content files</name>

  <read_first>
    - .planning/phases/06-ap-psychology-content/PLAN.md (PRIMARY — per-unit question plan from Content Planner; do not start without it)
    - .planning/phases/06-ap-psychology-content/RESEARCH.md (curriculum facts, key terms, psychologists, learning objectives)
    - docs/superpowers/specs/2026-03-26-drill-mcq-redesign.md (drill mode taxonomy, single canonical answer, is_key_term rules, concept_mc format, MCQ distractor quality, CB question language)
    - docs/PRD.md (data_schemas section — exact JSON structure for every file type)
    - data/schemas/drill.schema.json (canonical drill schema — additionalProperties: false)
    - data/schemas/study-guide.schema.json (canonical study guide schema)
    - .planning/CONTENT-WAVES.md (quality gates G1–G8, G8b, G8c, G8d, file wrapper structure)
    - CLAUDE.md (Critical Rules: KaTeX never as plain text, no alternate_answers, no scrambling in JSON)
    - .planning/phases/06-ap-psychology-content/reference/ap-psychology-course-and-exam-description.pdf (verify any content questions against PDF)
  </read_first>

  <files>
    public/data/ap-psychology/meta.json,
    public/data/ap-psychology/drills/unit-1.json,
    public/data/ap-psychology/drills/unit-2.json,
    public/data/ap-psychology/drills/unit-3.json,
    public/data/ap-psychology/drills/unit-4.json,
    public/data/ap-psychology/drills/unit-5.json,
    public/data/ap-psychology/drills/unit-6.json,
    public/data/ap-psychology/drills/unit-7.json,
    public/data/ap-psychology/drills/unit-8.json,
    public/data/ap-psychology/mcq/unit-1.json,
    public/data/ap-psychology/mcq/unit-2.json,
    public/data/ap-psychology/mcq/unit-3.json,
    public/data/ap-psychology/mcq/unit-4.json,
    public/data/ap-psychology/mcq/unit-5.json,
    public/data/ap-psychology/mcq/unit-6.json,
    public/data/ap-psychology/mcq/unit-7.json,
    public/data/ap-psychology/mcq/unit-8.json,
    public/data/ap-psychology/study-guide/unit-1.json,
    public/data/ap-psychology/study-guide/unit-2.json,
    public/data/ap-psychology/study-guide/unit-3.json,
    public/data/ap-psychology/study-guide/unit-4.json,
    public/data/ap-psychology/study-guide/unit-5.json,
    public/data/ap-psychology/study-guide/unit-6.json,
    public/data/ap-psychology/study-guide/unit-7.json,
    public/data/ap-psychology/study-guide/unit-8.json
  </files>

  <action>
    You are the Writer in the AP Psychology content pipeline. Read PLAN.md and RESEARCH.md before writing anything. Execute the plan mechanically — do not invent topics not covered in RESEARCH.md.

    **File order per unit (repeat for every unit):**
    1. drills/unit-N.json — all drill cards for the unit
    2. mcq/unit-N.json — all MCQ questions for the unit (split writes — see token limit)
    3. study-guide/unit-N.json — study guide
    Then move to the next unit. After all units: write meta.json.

    Create directories first if they don't exist:
    `mkdir -p public/data/ap-psychology/drills public/data/ap-psychology/mcq public/data/ap-psychology/study-guide`

    ---

    **TOKEN LIMIT — CRITICAL:**
    Write MCQ files INCREMENTALLY. Max 20–25 MCQs per Write call.
    For a unit with 60 MCQs: Write call 1 → questions 1–20, Write call 2 → questions 21–40, Write call 3 → questions 41–60.
    Start each subsequent Write call by reading the existing file first, then append the new batch to the array.
    Do NOT attempt to write all 50–100 MCQs in a single Write call — this causes a 32k token limit error.

    ---

    **Drill file format** (wrapper with cards array):
    ```json
    {
      "subject": "ap-psychology",
      "unit": "unit-N",
      "unit_name": "[official name from RESEARCH.md]",
      "cards": [ /* array of drill objects */ ]
    }
    ```

    **Drill rules:**
    - Approved modes for AP Psychology: definition_to_term, significance_to_person, concept_mc ONLY
    - FORBIDDEN modes (schema will reject): term_to_definition, significance_to_event, significance_to_case, name_to_formula, person_to_significance
    - typed:MC split target: 75:25 across the subject (not enforced per-unit but should be close)
    - is_key_term: true on exactly 8–15 cards per unit; only on typed-recall modes (never concept_mc)
    - No alternate_answers field — not in schema, will cause validation failure
    - concept_mc cards: answer field must be empty string ""; choices array required (3–4 items, each with text, is_correct, explanation)
    - Each distractor in concept_mc must explain the specific misconception it targets
    - ID format: psych-uN-d001, psych-uN-d002, ... (sequential, no gaps)

    **MCQ file format** (wrapper with questions array):
    ```json
    {
      "subject": "ap-psychology",
      "unit": "unit-N",
      "unit_name": "[official name from RESEARCH.md]",
      "questions": [ /* array of MCQ objects */ ]
    }
    ```

    **MCQ rules:**
    - Count: 50–100 questions per unit
    - Difficulty split: 20% easy / 45% medium / 35% hard (±5% tolerance)
    - Stimulus rate: ~70% of questions must have stimulus (type != "none")
    - Stimulus types for AP Psychology: text (research description, case study, experimental design scenario), table, no stimulus for foundational recall only
    - Choices: exactly 4 (ids A, B, C, D); exactly 1 is_correct: true
    - NEVER scramble choices in JSON — stored in A/B/C/D order, scrambled at render time only
    - Every choice must have an explanation (correct + each distractor). Distractor explanations must name the specific misconception.
    - Question language: College Board register. Use "Which of the following best explains…", "The study above most directly supports…", "A researcher who believes X would most likely predict…". Avoid "What is…" or "Name the…"
    - unit_objective field: use learning objective codes from RESEARCH.md (e.g. "PSY-1.A.1")
    - ID format: psych-uN-q001, psych-uN-q002, ... (sequential, no gaps, no duplicates)

    **Study guide file format** (single object, NO wrapper):
    ```json
    {
      "id": "psych-sg-unit-N",
      "unit": "unit-N",
      "subject": "ap-psychology",
      "theme": "[one sentence capturing unit's core theme]",
      "core_concepts": ["[5-8 sharp bullet points]"],
      "formulas": [],
      "exam_tip": "[what CB emphasizes from this unit]"
    }
    ```
    - core_concepts: 5–8 items; inline math uses $...$ KaTeX delimiters
    - AP Psychology rarely needs formulas array (omit or leave empty for text-heavy units)
    - NO key_terms array — removed from schema; key terms sourced from is_key_term drill cards at render time

    **meta.json format** (written LAST after all units are done):
    ```json
    {
      "subject_id": "ap-psychology",
      "subject_name": "AP Psychology",
      "exam_date": "[from RESEARCH.md]",
      "total_mcq_count": [sum of all unit MCQ counts],
      "units": [
        {
          "unit_id": "unit-1",
          "unit_name": "[from RESEARCH.md]",
          "unit_number": 1,
          "college_board_percentage": [from RESEARCH.md],
          "learning_objectives": ["PSY-1.A", "PSY-1.B"]
        }
      ]
    }
    ```

    **Quality self-check before each file write:**
    - Verify is_key_term count is 8–15 for the unit (drills)
    - Verify no alternate_answers field present in any card
    - Verify difficulty distribution is approximately 20/45/35 for the unit (MCQs)
    - Verify stimulus count is approximately 70% of questions (MCQs)
    - Verify all IDs follow psych-uN-d/q format and are sequential

    **IMPORTANT: The files_modified list above assumes 8 units (the prior web-based plan said 8). The actual unit count comes from RESEARCH.md / PLAN.md (derived from the CED PDF). If the PDF shows a different count, create the correct number of files and note the discrepancy. Unit count from PDF is authoritative.**
  </action>

  <verify>
    <automated>
      # Check meta.json exists and parses
      python3 -c "import json; d=json.load(open('public/data/ap-psychology/meta.json')); print('meta.json units:', len(d['units']))"

      # Check drill files exist for unit 1 through N
      for i in 1 2 3 4 5 6 7 8; do
        test -f "public/data/ap-psychology/drills/unit-${i}.json" && echo "drills/unit-${i}.json: OK" || echo "drills/unit-${i}.json: MISSING"
      done

      # Check MCQ counts per unit (all must be 50-100)
      for i in 1 2 3 4 5 6 7 8; do
        python3 -c "import json; d=json.load(open('public/data/ap-psychology/mcq/unit-${i}.json')); n=len(d['questions']); print(f'mcq/unit-${i}.json: {n} questions', 'OK' if 50<=n<=100 else 'FAIL')" 2>/dev/null || echo "mcq/unit-${i}.json: MISSING"
      done

      # Check drill card counts (all must be >= 20)
      for i in 1 2 3 4 5 6 7 8; do
        python3 -c "import json; d=json.load(open('public/data/ap-psychology/drills/unit-${i}.json')); n=len(d['cards']); print(f'drills/unit-${i}.json: {n} cards', 'OK' if n>=20 else 'FAIL')" 2>/dev/null || echo "drills/unit-${i}.json: MISSING"
      done

      # Check no alternate_answers field anywhere
      grep -r "alternate_answers" public/data/ap-psychology/ && echo "FAIL: alternate_answers found" || echo "PASS: no alternate_answers"

      # Check is_key_term count per unit (must be 8-15)
      for i in 1 2 3 4 5 6 7 8; do
        python3 -c "import json; d=json.load(open('public/data/ap-psychology/drills/unit-${i}.json')); n=sum(1 for c in d['cards'] if c.get('is_key_term')); print(f'unit-${i} is_key_term: {n}', 'OK' if 8<=n<=15 else 'FAIL')" 2>/dev/null
      done

      # Check difficulty distribution (20/45/35) for unit 1 as sample
      python3 -c "
      import json
      d=json.load(open('public/data/ap-psychology/mcq/unit-1.json'))
      qs=d['questions']
      total=len(qs)
      easy=sum(1 for q in qs if q['difficulty']=='easy')
      med=sum(1 for q in qs if q['difficulty']=='medium')
      hard=sum(1 for q in qs if q['difficulty']=='hard')
      print(f'Unit 1 difficulty: easy={easy/total:.0%} med={med/total:.0%} hard={hard/total:.0%}')
      print('OK' if 0.15<=easy/total<=0.25 and 0.40<=med/total<=0.50 and 0.30<=hard/total<=0.40 else 'FAIL: distribution off')
      " 2>/dev/null

      # Check stimulus rate for unit 1 as sample (~70% should have stimulus)
      python3 -c "
      import json
      d=json.load(open('public/data/ap-psychology/mcq/unit-1.json'))
      qs=d['questions']
      total=len(qs)
      with_stim=sum(1 for q in qs if q.get('stimulus',{}).get('type','none')!='none')
      print(f'Unit 1 stimulus rate: {with_stim/total:.0%}')
      print('OK' if with_stim/total>=0.60 else 'FAIL: stimulus rate below 60% (target 70%)')
      " 2>/dev/null
    </automated>
  </verify>

  <acceptance_criteria>
    - meta.json exists and parses as valid JSON: `python3 -c "import json; json.load(open('public/data/ap-psychology/meta.json'))"`
    - meta.json has correct number of units (matching RESEARCH.md): `python3 -c "import json; d=json.load(open('public/data/ap-psychology/meta.json')); print(len(d['units']), 'units')"`
    - All drill files exist: `ls public/data/ap-psychology/drills/unit-*.json | wc -l` equals unit count
    - All MCQ files exist: `ls public/data/ap-psychology/mcq/unit-*.json | wc -l` equals unit count
    - All study guide files exist: `ls public/data/ap-psychology/study-guide/unit-*.json | wc -l` equals unit count
    - Every drill file has >= 20 cards: `python3 -c "import json,glob; [print(f,len(json.load(open(f))['cards'])) for f in glob.glob('public/data/ap-psychology/drills/*.json')]"`
    - Every MCQ file has 50-100 questions: `python3 -c "import json,glob; [print(f,len(json.load(open(f))['questions'])) for f in glob.glob('public/data/ap-psychology/mcq/*.json')]"`
    - No alternate_answers field anywhere: `grep -r "alternate_answers" public/data/ap-psychology/` returns empty
    - Every unit has 8–15 is_key_term: true drill cards (none on concept_mc mode)
    - Difficulty distribution per unit approximately 20/45/35 (±5%)
    - MCQ stimulus rate approximately 70% per unit (type != "none")
    - All MCQ choice IDs are A, B, C, D (not 0, 1, 2, 3 and not positional labels)
    - All drill IDs follow psych-uN-dNNN format; all MCQ IDs follow psych-uN-qNNN format
    - No duplicate IDs within the subject (all IDs unique across all unit files)
  </acceptance_criteria>

  <done>
    All AP Psychology content files written to public/data/ap-psychology/. Every unit has drills (≥20 cards), MCQs (50–100, 20/45/35 split, ~70% stimulus), and a study guide. meta.json covers all units with CB percentages and learning objectives. No alternate_answers, no forbidden drill modes, correct is_key_term counts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reviewer (Sonnet) — Validate all quality gates and sign off</name>

  <read_first>
    - .planning/phases/06-ap-psychology-content/RESEARCH.md (curriculum reference for accuracy checks)
    - .planning/phases/06-ap-psychology-content/PLAN.md (what the Writer was supposed to produce)
    - docs/superpowers/specs/2026-03-26-drill-mcq-redesign.md (the checklist of what to verify — Section 5)
    - docs/PRD.md (data_schemas section — verify structure compliance)
    - data/schemas/drill.schema.json (validate every drill card)
    - data/schemas/study-guide.schema.json (validate every study guide)
    - .planning/CONTENT-WAVES.md (quality gates table G1–G8, G8b, G8c, G8d)
    - CLAUDE.md (Critical Rules — verify KaTeX, scramble, per-choice explanation rules)
  </read_first>

  <files>
    public/data/ap-psychology/meta.json,
    public/data/ap-psychology/drills/unit-1.json through unit-N.json,
    public/data/ap-psychology/mcq/unit-1.json through unit-N.json,
    public/data/ap-psychology/study-guide/unit-1.json through unit-N.json
  </files>

  <action>
    You are the Reviewer (model: Sonnet — NOT Opus; Opus is only for Calculus AB and Chemistry) in the AP Psychology content pipeline. Run ALL quality gates below against every file. For each gate: PASS or report exact file, array index, field, and required fix. Return to Writer if any gate fails — do not approve partial compliance.

    **Gate G1 — Schema Compliance:**
    Validate every drill card against data/schemas/drill.schema.json. Validate every study guide against data/schemas/study-guide.schema.json. Check: required fields present, no additionalProperties (schema has additionalProperties: false), enum values valid (mode must be one of the 6 approved modes).

    **Gate G2 — Difficulty Distribution:**
    Per unit MCQ file: 20% easy ±5%, 45% medium ±5%, 35% hard ±5%. Calculate actual percentages. Report any unit outside tolerance.

    **Gate G3 — MCQ Count:**
    Every MCQ unit file must have 50–100 questions. Count and report.

    **Gate G4 — Drill Coverage:**
    Cross-reference drill cards against PLAN.md drill coverage list. Every item in the plan's drill coverage list must be represented by at least one card in the unit. Report missing terms/psychologists/concepts.

    **Gate G5 — KaTeX:**
    AP Psychology is text-heavy; minimal formulas expected. Check study guide core_concepts and exam_tip fields for any raw math notation (e.g. subscripts written as H2O without KaTeX where it should be $\text{H}_2\text{O}$). Report any plain-text math.

    **Gate G6 — Explanations:**
    Every MCQ choice must have a non-empty explanation. Every concept_mc choice must have a non-empty explanation. Explanations for wrong choices must name the specific misconception (not just "This is incorrect"). Sample-check 10 MCQ questions per unit and all concept_mc cards.

    **Gate G7 — Correct Answers:**
    Each MCQ must have exactly 1 choice with is_correct: true. Each concept_mc card must have exactly 1 choice with is_correct: true. Verify with count. Report any question with 0 or 2+ correct answers.
    Check: no alternate_answers field anywhere. Run: `grep -r "alternate_answers" public/data/ap-psychology/` — must return empty.

    **Gate G8 — ID Uniqueness:**
    Collect all drill IDs across all unit files. Collect all MCQ IDs across all unit files. Check for duplicates. Report any duplicate ID with both file locations.

    **Gate G8b — is_key_term Rules:**
    Per unit: count cards where is_key_term: true. Must be 8–15. Check that no concept_mc card has is_key_term: true. Report violations.

    **Gate G8c — Drill Modes:**
    Every drill card mode must be one of: definition_to_term, significance_to_person, concept_mc.
    Forbidden modes: term_to_definition, significance_to_event, significance_to_case, name_to_formula, person_to_significance, formula_to_type, event_to_date, concept_to_example.
    Report any card with a forbidden mode.

    **Gate G8d — MCQ Stimulus Rate:**
    Per unit: count questions where stimulus.type != "none". Target is ~70%. Report any unit below 60% (hard floor; target is 70%).

    **Curriculum Accuracy Spot-Check:**
    For 3–5 MCQs per unit: verify the correct answer matches the CED PDF content (cross-reference RESEARCH.md). Flag any factually incorrect question (wrong person credited for a discovery, wrong definition, etc.).

    **Output format:**
    After running all gates:
    - If ALL gates pass: write "REVIEWER APPROVED — AP Psychology content passes all quality gates G1–G8, G8b, G8c, G8d. Ready for commit." to stdout and to a file `.planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md`
    - If ANY gate fails: write a rejection report listing every failure with exact location (file, question index/card index, field). The Writer must fix all failures before re-review. Do NOT approve partial compliance.
  </action>

  <verify>
    <automated>
      # Reviewer sign-off file must exist
      test -f ".planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md" && echo "PASS: REVIEWER-SIGNOFF.md exists" || echo "FAIL: Reviewer has not produced sign-off"
      # Sign-off must contain approval string (not a rejection)
      grep -q "REVIEWER APPROVED" ".planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md" && echo "PASS: Content approved" || echo "FAIL: Review not approved — check REVIEWER-SIGNOFF.md"
      # Final check: no alternate_answers in output
      grep -r "alternate_answers" public/data/ap-psychology/ && echo "FAIL: alternate_answers found post-review" || echo "PASS: no alternate_answers"
    </automated>
  </verify>

  <acceptance_criteria>
    - REVIEWER-SIGNOFF.md exists at `.planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md`
    - REVIEWER-SIGNOFF.md contains "REVIEWER APPROVED" (not a rejection report)
    - All 10 quality gates (G1–G8, G8b, G8c, G8d) explicitly listed as PASS in sign-off
    - No alternate_answers field anywhere: `grep -r "alternate_answers" public/data/ap-psychology/` returns empty
    - All drill files have 8–15 is_key_term: true cards per unit
    - All MCQ files have 50–100 questions with 20/45/35 ±5% difficulty split
    - Stimulus rate ≥60% per unit (target 70%)
    - No forbidden drill modes (term_to_definition, etc.) in any drill file
    - All IDs unique within subject (no duplicates)
  </acceptance_criteria>

  <done>
    Reviewer has run all quality gates G1–G8, G8b, G8c, G8d and produced REVIEWER-SIGNOFF.md containing "REVIEWER APPROVED". All content issues resolved. AP Psychology content is ready for commit to branch content/ap-psychology.
  </done>
</task>

</tasks>

<verification>
After both tasks complete and Reviewer has approved:

1. All data files exist: `ls public/data/ap-psychology/**/*.json | wc -l` (should be: N drills + N MCQs + N study guides + 1 meta = 3N+1)
2. No alternate_answers: `grep -r "alternate_answers" public/data/ap-psychology/` returns empty
3. is_key_term counts valid per unit: `python3 -c "import json,glob; [print(f, sum(1 for c in json.load(open(f))['cards'] if c.get('is_key_term'))) for f in sorted(glob.glob('public/data/ap-psychology/drills/*.json'))]"`
4. MCQ counts valid per unit: `python3 -c "import json,glob; [print(f, len(json.load(open(f))['questions'])) for f in sorted(glob.glob('public/data/ap-psychology/mcq/*.json'))]"`
5. Reviewer approved: `grep "REVIEWER APPROVED" .planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md`
</verification>

<success_criteria>
- meta.json: valid, all units with CB percentages and learning objectives
- Drills: all unit files present, ≥20 cards each, 75:25 typed:MC split, 8-15 is_key_term per unit, no alternate_answers, only approved modes (definition_to_term, significance_to_person, concept_mc)
- MCQs: all unit files present, 50-100 questions each, 20/45/35 difficulty split ±5%, ~70% stimulus rate, all choices have explanations, exactly 1 correct choice per question, CB question language
- Study guides: all unit files present, 5-8 core_concepts, exam_tip, no key_terms array
- Reviewer sign-off: REVIEWER-SIGNOFF.md with "REVIEWER APPROVED" and all gates passing
- Ready to commit to branch content/ap-psychology
</success_criteria>

<output>
After completion, create `.planning/phases/06-ap-psychology-content/06-02-SUMMARY.md`

Then commit to branch `content/ap-psychology`:
```
git checkout -b content/ap-psychology 2>/dev/null || git checkout content/ap-psychology
git add public/data/ap-psychology/
git add .planning/phases/06-ap-psychology-content/RESEARCH.md
git add .planning/phases/06-ap-psychology-content/PLAN.md
git add .planning/phases/06-ap-psychology-content/meta-draft.json
git add .planning/phases/06-ap-psychology-content/REVIEWER-SIGNOFF.md
git commit -m "feat(content): AP Psychology — all units drills, MCQs, study guides (G1-G8 passed)"
```
</output>
